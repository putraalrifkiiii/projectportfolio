container. mengatur max-width sesuai ukuran layar (responsive)
mx-auto. membuatnya di tengah (center)
left -1/2. Elemen digeser dari sisi kiri sebesar 50% dari parent-nya
left-1/2 -translate-x-1/2. posisi agar item tepat ditengah
window.addEventListener("scroll", handleScroll). setiap scroll, jalankan handle scroll
}, []); . jalankan effect hanya sekali saat component pertama muncul (mount), tidak peduli jika ada update
top hanya bekerja kalau:position relative, absolute, fixed, sticky
bg-white/30. untuk transparan
threshold. batas scroll
w-fit. lebar elemen menyesuaikan isi kontennya.
/tight. Line-height rapat
/loose. line-height longgar
50vw. 50% dari lebar layar
key. dipakai React untuk membedakan setiap element dalam list agar rendering lebih cepat.
loading="lazy". menunda pemuatan (load) gambar atau iframe sampai hampir terlihat di layar pengguna.
noopener. Mencegah halaman baru mengontrol halaman asal.
noreferrer. Tidak mengirim informasi asal website ke website tujuan.
origin = alamat repository GitHub yang terhubung dengan project kamu
fetch artinya mengambil update dari GitHub ke komputer kamu, Tapi perubahan itu belum masuk ke kode kamu, hanya di-download saja.
